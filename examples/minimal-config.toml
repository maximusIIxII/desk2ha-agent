# Desk2HA Agent — Minimal Configuration
# Only HTTP transport with a token is required.
#
# Generate a secure token:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"

[http]
enabled = true
auth_token = "CHANGE_ME_TO_A_RANDOM_TOKEN"
